<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Listado de Áreas</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: #f4f8fb;
            color: #2d3e50;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 40px auto;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(44, 62, 80, 0.08);
            padding: 32px;
        }
        h1 {
            color: #274472;
            margin-bottom: 24px;
            text-align: center;
            font-weight: 700;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            background: #f8fafc;
        }
        th, td {
            padding: 14px 12px;
            text-align: left;
        }
        th {
            background: #e3eaf2;
            color: #274472;
            font-weight: 700;
            border-bottom: 2px solid #bfc9d9;
        }
        tr:nth-child(even) {
            background: #eaf1f8;
        }
        tr:hover {
            background: #d6e4f0;
        }
        .empty {
            text-align: center;
            color: #7b8ca7;
            padding: 32px 0;
        }
    </style>
</head>
{{> navbar}}
<body>
    <div class="container-app">
    {{> sidebar}}
        <div class="main-content" style="justify-content: flex-start;">
            <div class="container">
                <h1>{{titulo}}</h1>
                {{#if perspectivas.length}}
                <table>
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Descripción</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each perspectivas}}
                        <tr>
                            <td>{{@index}}</td>
                            <td>{{this.descripcion}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                {{else}}
                <div class="empty">No hay perspectivas registradas.</div>
                {{/if}}
            </div>
        </div>
    </div>
</body>
</html>