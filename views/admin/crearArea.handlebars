<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Crear Nueva Área</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap" rel="stylesheet">
    <style>
        body {
            background: #f4f7fa;
            font-family: 'Roboto', sans-serif;
            color: #2c3e50;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 420px;
            margin: 60px auto;
            background: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 24px rgba(44, 62, 80, 0.08);
            padding: 32px 36px;
        }
        h2 {
            text-align: center;
            margin-bottom: 28px;
            color: #34495e;
            font-weight: 700;
        }
        label {
            display: block;
            margin-bottom: 8px;
            color: #2980b9;
            font-weight: 500;
        }
        input[type="text"] {
            width: 100%;
            padding: 10px 12px;
            border: 1px solid #b2bec3;
            border-radius: 4px;
            margin-bottom: 20px;
            font-size: 16px;
            background: #ecf0f1;
            color: #2c3e50;
        }
        button {
            width: 100%;
            padding: 12px;
            background: #2980b9;
            color: #fff;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            font-weight: 700;
            cursor: pointer;
            transition: background 0.2s;
        }
        button:hover {
            background: #2471a3;
        }
    </style>
</head>
{{> navbar}}
<body>
    <div class="container-app">
    {{> sidebar}}
        <div class="main-content" style="justify-content: flex-start;">
            <div class="container">
                <h2>Crear Nueva Área</h2>
                <form action="/admin/nuevaArea" method="POST">
                    <label for="nombre">Nombre del Área</label>
                    <input type="text" id="nombre" name="nombre" required placeholder="Ej: Educacion Fisica">
                    <button type="submit">Crear Área</button>
                </form>
            </div>
        </div>
    </div>
    {{#if message}}
        <script>
            Swal.fire({
                icon: 'info',
                title: 'Mensaje',
                text: '{{message}}'
            });
        </script>
    {{/if}}
</body>
</html>